on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
     - uses: actions/checkout@v3


     - name:  Login to Docker hub"
       run: docker login --username doaa6ismail --password dckr_pat_b8-alkrfCV-4c3eRL8EevSQDBV8

